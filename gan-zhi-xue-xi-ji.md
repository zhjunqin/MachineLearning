# **感知机**

感知机\(Peceptron\)是二类分类的线性分类模型，其输入为实例的特征向量，输出为实例的类别，取$$+1$$和$$-1$$二值。感知机将对应于输入空间（特征空间）中将实例划分为正负的分离超平面，属于判别模型。

### 1. 定义

假设输入空间（特征空间）是$$X \subseteq R^n$$，输出空间是$$Y=\lbrace+1,-1\rbrace$$。输入$$x\in X$$表示实例的特征向量，对应于输入空间（特征空间）的点：输出$$y\in Y$$表示实例的类别。由输入空间到输出空间的如下函数：


$$
f(x)=sign(w\cdot x+b)
$$


称为感知机。其中，$$w$$和$$b$$为感知模型参数，$$w\in R^n$$叫做权值\(weight\)或权值向量\(weight vector\)，$$b\in R$$叫做偏置，$$w\cdot x$$表示$$w$$和$$x$$的内积。$$sign$$是符号函数，即


$$
sign(x) = \begin{cases}
   +1 &\text{if } x>=0  \\
   -1 &\text{if } x<0
\end{cases}
$$


感知机是一种线性分类模型，属于判别模型。

感知模型的假设空间是定义在特征空间中的所有线性分类模型\(linear classification model\)或线性分类器\(linear classifier\)，即函数集合$$\{f\vert f(x)=w\cdot x+b\}$$。

### 2.几何解释

线性方程$$w\cdot x+b=0$$对应于特征空间$$R^n$$中的一个超平面$$S$$，其中$$w$$是超平面的法向量，$$b$$是超平面的截距。这个超平面将特征空间划分为两部分。位于两部分的点（特征向量）分别被分为正负两类，因此，超平面$$S$$称为分离超平面\(separating hyperplane\)，如图所示。

![](/assets/感知机模型)

其中超平面上的任意两个点，比如为$$x_1$$,$$x_2$$满足方程


$$
\begin{gathered}
   w\cdot x_1 = -b \\ 
   w\cdot x_2 = -b
\end{gathered}
$$


则$$w\cdot (x_1-x_2)=0$$，也就是超平面上任意两个点的构成的直线与$$w$$的内积为$$0$$，则互相垂直。对于超平面来讲$$w$$的方向并不重要，只需要垂直于超平面即可。

满足$$w\cdot x+b>0$$的的点位于超平面跟$$w$$的方向一致的一面，满足$$w\cdot x+b<0$$的点位于超平面跟$$w$$方向相反的一面。因为取超平面上任意一点假设为$$x_0$$，则超平面外的任何一点$$x$$满足$$w\cdot (x-x_0)>0$$，则说明这两点构成的直线跟$$w$$的夹角小于90度，反之小于0，则夹角大于90度。

